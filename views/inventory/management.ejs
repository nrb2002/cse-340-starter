<!-- /*************************** 
 * Deliver management view  
 * *************************/ 
-->
<!-- Check to see if a title variable exists -->
<% if (title) { %>
    <!-- if a title variable exists, then display it as an h1 -->
    <h1 class="title"><%= title %></h1>
<% } else {
    // Otherwise, redirect the response obkect to the home view
    res.redirect('/')
} %>

<!-- Flash notifications -->
<%- messages() %>

<!-- This is the main content to be displayed in the view -->

<div class="management">
   <a class="btn" href="/inv/add-classification">Add New Classification</a>
   <a class="btn" href="/inv/add-inventory">Add New Vehicle</a>
</div>

<!-- <div class="form-container">
    <div class="form-card">
        <h2>Inventory Management Form</h2><hr> 

        <form class="account-form" id="managementForm" action="/account/management" method="post">
    
            <p><small>All fields are required.</small></p><br><br>
        
            <label for="account_firstname">Firstname</label>
            <input type="text" id="account_firstname" name="account_firstname" placeholder="Enter your Firstname" value="<%= locals.account_firstname %>" required>
            
            <label for="account_lastname">Lastname</label>
            <input type="text" id="account_lastname" name="account_lastname" placeholder="Enter your Lastname" value="<%= locals.account_lastname %>" required>
            
            <label for="account_email">Email</label>
            <input type="email" id=account_email" name="account_email" placeholder="Enter your email address" value="<%= locals.account_email %>" required>
        
            <label for="account_password">Password</label>
            <input type="password" pattern="^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[^a-zA-Z0-9])(?!.*\s).{12,}$" id="account_password" name="account_password" placeholder="Enter your password" required>
            <small>The password must contain at least 12 characters, 1 capital letter, 1 number, and 1 special character.</small>
        
            <label for="account_confirm_password">Confirm Password</label>
            <input type="password" pattern="^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[^a-zA-Z0-9])(?!.*\s).{12,}$" id="account_confirm_password" name="account_confirm_password" placeholder="Confirm your password" required>
            <label class="togglePasswd"><input type="checkbox" onclick="togglePassword()">Show password</label>
        
            <label for="account_type">Account Type</label>
            <select name="account_type" id="account_type" required>
                <option value="">-- Select a type</option>
                <option value="Client">Client</option>
                <option value="Admin">Admin</option>
            </select>
        
            <input class="btn" type="submit" value="Sign up">
        </form>

        <div class="form-link">Already have an account? <a href="/account/login" >Sign in here.</a></div>
    
    </div>
</div> -->



