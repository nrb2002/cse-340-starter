<!-- EJS code block to contain a JavaScript if structure to determine if a "title" variable exists. If the variable exists, displays within an <h1> element.
-->
<% if (title) { %> 
    <h1>
        <%= title %>
    </h1>
<% } else { res.redirect('/') } %> <!-- otherwise, redirect the response object to the default index route, to deliver the home view.-->

<!-- Message section.  -->
<%# messages() %>

<!-- This code block is the main content delivery mechanism for the view as it displays the vehicles' grid -->
<%- grid %>